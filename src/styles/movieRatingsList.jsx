import styled from 'styled-components';

const StyledRatingsList = styled.div`
  display: flex;
  justify-content: space-between;
  flex-wrap: wrap;
  min-height: 5em;
  text-align: center;
`;

export default StyledRatingsList;
